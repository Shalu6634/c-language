#include<stdio.h>
main()
{
 //WAP to Find min. no. from given 2 no. using if else.
  int a,b;
  printf("enter the value of a,b:");
  scanf("%d%d",&a,&b);
  
  if(a<b)
  {
  	printf("a is minimum ");
  }
  else
  {
  	printf("b is minimum");
  }
}